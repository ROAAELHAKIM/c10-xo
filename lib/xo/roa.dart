import 'package:flutter/material.dart';

class roaa extends StatelessWidget {
  const roaa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("fuckk"),),
    );
  }
}
